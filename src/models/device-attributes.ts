/**
 * Attributes (functions) for each device type
 */
export enum DeviceAttribute{
    // Light attributes
    LightPower = 2,
    LightBrightness = 4,
    // Fan attributes
    FanPower = 3,
    FanSpeed = 6
}